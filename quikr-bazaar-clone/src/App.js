import "./App.css";
// import { applyDarkTheme, applyLightTheme } from "./redux/theme/theme.actions";
import { useDispatch, useSelector } from "react-redux";
// import { useEffect } from "react";
// import { getProducts } from "./redux/products/products.actions";

function App() {
  // const dispatch = useDispatch();
  // This is a smaple how you can use a redux state or dispatch anything

  // const theme = useSelector((store) => store.Theme.theme);
  // const { loading, error, data } = useSelector((store) => store.Products);
  // const authOrNot = useSelector((store) => store.Auth)

  // useEffect(() => {
  //   dispatch(getProducts());
  // }, []);

  return (
    <div>
      <p>Unit-5 UNIT-6</p>
    </div>
  );
}

export default App;
